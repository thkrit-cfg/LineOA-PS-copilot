// PLACEHOLDER — replaced at build time.
// `npm run build:api` (run by Vercel's buildCommand) bundles server/entry.ts
// into this file with esbuild, producing a fully self-contained Express app
// with zero local-file imports. Vercel detects this file as the /api function
// and builds it from the bundled output.
export default {};
