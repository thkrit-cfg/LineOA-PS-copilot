// PLACEHOLDER — replaced at build time.
// `npm run build:api` (run by Vercel's buildCommand) bundles server/entry.ts
// into this file with esbuild (CJS), producing a fully self-contained
// Express app with zero local-file imports. Vercel detects this file as the
// /api function. .js + api/package.json (type:commonjs) => loaded as CJS.
module.exports = {};
